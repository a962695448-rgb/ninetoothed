# 整数常量子树折叠：未采用

候选仅在计划构建时折叠有界的精确 Python int/bool 子树；生产基线为 `4d50b0424d963ac7ea3fd67fb06ff2c828fb3d92`。

## 结论

三轮独立进程对照均未达到预先冻结的 1.10× 几何平均加速门槛。保留源码、失败测试脚本初稿和全部原始记录；不发布该求值实现。

| 轮次 | 三种 matmul 目标几何平均加速比 | 门槛 |
|---|---:|---:|
| 1 | 1.014061× | 1.10× |
| 2 | 1.002081× | 1.10× |
| 3 | 1.004981× | 1.10× |

完整 CPU 选择范围：`770 passed, 15 deselected in 34.84s`；表达式专项：`51 passed in 0.55s`。33 项配对的轨迹摘要相同，7 种完整前端轨迹逐轮相同。正确性通过不等于性能达标。

最初随机树参考求值遇到 NumPy 布尔减法 TypeError，测试未比较异常，产生 8 failed / 43 passed。修订测试以比较相同异常类型和信息；原脚本、日志和 JUnit 保留于 `results/initial-test-harness/`，没有删除失败用例。

冷启动和 tracemalloc 数据记录在原始 JSON 中，未用来替代稳态门槛。本轮没有启动 GPU，也不对 GPU 性能作出声明。

后续实验位于同级 `literal-plans-20260917`：取消常量预求值，只减少常量叶节点的返回函数调用，仍然在运行时调用全部算子。
